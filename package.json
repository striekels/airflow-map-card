{
  "name": "airflow-map-card",
  "version": "0.2.1",
  "description": "Home Assistant Lovelace card showing wind direction over your house on an OpenStreetMap basemap.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && node scripts/build.mjs",
    "build:analyze": "node scripts/build.mjs --analyze",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src test --ext .ts",
    "format": "prettier --write \"{src,test,dev}/**/*.{ts,html,css}\""
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.16",
    "@typescript-eslint/eslint-plugin": "^8.24.0",
    "@typescript-eslint/parser": "^8.24.0",
    "esbuild": "^0.25.12",
    "eslint": "^8.57.1",
    "prettier": "^3.5.1",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  }
}
